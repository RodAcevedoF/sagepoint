{
  "ignoreCommand": "bash -c ': \"Restore staging previews by changing the branch gate back to develop|master.\"; case \"$VERCEL_GIT_COMMIT_REF\" in master) ;; *) exit 0 ;; esac; git diff --quiet HEAD~1 HEAD -- . && exit 0; exit 1'"
}
